name: "first-workflow"
on: push

jobs: 
    first-job:
        run-on: ubuntu-latest
        steps:
            - name: checkout
              uses: action/checkout@v4
              with: 
                node-version: 24
                cache: npm

            - name: install dependency
              run: npm ci

            - name: make react build
              run: npm run build